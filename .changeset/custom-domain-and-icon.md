---
"authifi-docs": minor
---

Set `docs.authifi.io` as the canonical custom domain across the site (`site_url`, robots/sitemap, `.well-known` agent assets, and security docs), document the Cloudflare Pages custom-domain DNS setup, and add the Authifi logo as the header logo and favicon.
